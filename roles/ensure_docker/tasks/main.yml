- name: Install Docker dependencies and packages
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - docker.io
      - docker-compose-v2
    state: present
    update_cache: true

- name: Enable and start Docker
  ansible.builtin.service:
    name: docker
    enabled: true
    state: started
